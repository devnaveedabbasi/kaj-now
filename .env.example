# AWS S3 media storage (server-side only; never expose these values to clients)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=
AWS_S3_BUCKET=

# Existing required application settings
MONGODB_URI=
JWT_SECRET=
PORT=5000
